# 🚀 كيفية تفعيل التحديث التلقائي عبر GitHub

لقد قمت بإعداد التطبيق ليدعم التحديث التلقائي. لإكمال العملية، اتبع الخطوات التالية:

## 1️⃣ إعداد GitHub
1. أنشئ حساباً على [GitHub.com](https://github.com) (إذا لم يكن لديك).
2. أنشئ **مستودعاً جديداً (New Repository)**.
   - اجعله **Public** (عام) ليعمل التحديث التلقائي مجاناً وبسهولة.

## 2️⃣ ربط التطبيق بالمستودع
1. افتح ملف `package.json`.
2. ابحث عن الجزء:
   ```json
   "publish": [
     {
       "provider": "github",
       "owner": "YOUR_GITHUB_USERNAME",
       "repo": "YOUR_REPO_NAME"
     }
   ]
   ```
3. استبدل `YOUR_GITHUB_USERNAME` باسم المستخدم الخاص بك.
4. استبدل `YOUR_REPO_NAME` باسم المستودع الذي أنشأته.

## 3️⃣ رفع الكود (Push)
افتح موجه الأوامر (Terminal) في مجلد المشروع ونفذ:

```bash
initgit 
git add .
git commit -m "First release"
git branch -M main
git remote add origin https://github.com/اسم_المستخدم/اسم_المستودع.git
git push -u origin main
```

## 4️⃣ نشر تحديث جديد (Release)

### الطريقة اليدوية (الأسهل):
1. قم ببناء التطبيق:
   ```bash
   npm run build:win
   ```
2. اذهب إلى مجلد `dist` الذي تم إنشاؤه.
3. اذهب إلى صفحة المستودع على GitHub > قسم **Releases** > **Draft a new release**.
4. في خانة Tag version، اكتب الإصدار (مثلاً `v1.0.0`).
5. ارفع الملفات التالية من مجلد `dist`:
   - `HyMods Launcher Setup 1.0.0.exe`
   - `latest.yml` (مهم جداً!)
6. اضغط **Publish release**.

🛑 **ملاحظة:** في المرة القادمة، عندما تريد إصدار تحديث، غيّر الرقم في `package.json` (مثلاً إلى `1.0.1`)، ثم ابنِ التطبيق وارفعه بنفس الطريقة. سيقوم التطبيق لدى المستخدمين باكتشاف التحديث وتنزيله تلقائياً!
